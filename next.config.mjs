/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'conference-alert.pockethost.io',
        'images.unsplash.com',
        'plus.unsplash.com',
        
      ],
    },
};

export default nextConfig;
